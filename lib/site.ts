/**
 * Where this site lives, in one place.
 *
 * It is written into the canonical links, the sitemap, the robots file and
 * the base every social card's image URL is resolved against — four places
 * that have to agree, and that fail quietly rather than loudly when they do
 * not: a wrong host here does not break a page, it just serves a share card
 * that nobody can load and a sitemap search engines discard.
 *
 * Change it here and everything follows.
 */
export const SITE_URL = "https://www.nikolaanastasijevic.com";
