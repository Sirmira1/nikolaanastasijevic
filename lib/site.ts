/**
 * Where this site lives, in one place.
 *
 * It is written into the canonical link, the sitemap, the robots file and the
 * base every social-card image URL resolves against — four things that have to
 * agree, and that fail quietly rather than loudly when they do not: a wrong
 * host here does not break a page, it just serves a share card nobody can load
 * and a sitemap search engines discard.
 */
export const SITE_URL = "https://www.nikolaanastasijevic.com";
